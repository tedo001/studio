rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Global access to ensure interconnected modules work without permission errors.
    // In production, these should be restricted to specific roles/UIDs.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}