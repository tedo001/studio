rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Global access for all authenticated users.
    // This ensures Citizen, Admin, and Worker modules can interoperate without permission errors.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}